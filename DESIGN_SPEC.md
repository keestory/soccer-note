# SoccerNote - 디자인 사양

## 1. 디자인 원칙

### MVP 디자인 철학
- **심플**: 불필요한 장식 제거, 축구 그라운드 중심
- **기능 중심**: 빠른 선수 배치와 점수 입력
- **일관성**: 쿼터별 동일한 UI 패턴
- **접근성**: 터치/마우스 모두 지원

### 디자인 목표
1. 직관적인 드래그 앤 드롭 포메이션 배치
2. 쿼터별 빠른 점수 입력 (1분 이내)
3. MVP 한눈에 확인

---

## 2. 컬러 시스템

### 컬러 팔레트 (Tailwind CSS 기준)

#### Primary Colors (축구장 그린)
```css
primary-50:  #ECFDF5   /* 매우 밝음 - 선택된 카드 배경 */
primary-100: #D1FAE5   /* 밝음 - hover 배경 */
primary-500: #10B981   /* 기본 - 주요 버튼, 필드 잔디 */
primary-600: #059669   /* 진함 - hover 상태 */
primary-700: #047857   /* 매우 진함 - active 상태 */
```

**Tailwind 매핑**: emerald 팔레트 사용
```javascript
// tailwind.config.js
colors: {
  primary: colors.emerald,
  field: '#2D5A27',  // 축구장 잔디색
}
```

#### Neutral Colors (회색 스케일)
```css
gray-50:  #F9FAFB   /* 배경 */
gray-100: #F3F4F6   /* 카드 배경 */
gray-200: #E5E7EB   /* 테두리 */
gray-400: #9CA3AF   /* 비활성 텍스트 */
gray-600: #4B5563   /* 보조 텍스트 */
gray-900: #111827   /* 주요 텍스트 */
```

#### 포지션별 컬러
```css
/* 골키퍼 */
gk: #F59E0B (amber-500) - 노란색

/* 수비수 */
df: #3B82F6 (blue-500) - 파란색

/* 미드필더 */
mf: #10B981 (emerald-500) - 초록색

/* 공격수 */
fw: #EF4444 (red-500) - 빨간색
```

#### Semantic Colors
```css
success: #10B981  /* 저장 완료, MVP 표시 */
warning: #F59E0B  /* 미입력 경고 */
error:   #EF4444  /* 에러 */
mvp:     #FBBF24  /* MVP 배지 (gold) */
```

### 사용 예시
| 용도 | 컬러 | Tailwind Class |
|------|------|----------------|
| 축구장 배경 | Field Green | bg-[#2D5A27] |
| 필드 라인 | White | border-white |
| 선수 마커 | Position Color | bg-amber-500 (GK) |
| MVP 배지 | Gold | bg-amber-400 text-amber-900 |

---

## 3. 타이포그래피

### 폰트 패밀리
```css
font-sans: 'Pretendard', ui-sans-serif, system-ui, sans-serif
font-mono: 'JetBrains Mono', ui-monospace /* 점수 표시 */
```

### 타이포그래피 스케일
| 레벨 | 용도 | 크기 | Tailwind |
|------|------|------|----------|
| H1 | 페이지 타이틀 | 30px | text-3xl font-bold |
| H2 | 섹션 헤딩 | 24px | text-2xl font-semibold |
| H3 | 카드 타이틀 | 20px | text-xl font-semibold |
| Body | 일반 본문 | 16px | text-base |
| Small | 보조 정보 | 14px | text-sm |
| Caption | 캡션 | 12px | text-xs |
| Score | 점수 표시 | 24px | text-2xl font-mono font-bold |

---

## 4. 간격 시스템 (Spacing)

### 기본 스케일
| 간격 | 픽셀 | Tailwind | 용도 |
|------|------|----------|------|
| 2 | 8px | p-2 / gap-2 | 아이콘 간격 |
| 3 | 12px | p-3 | 버튼 패딩 |
| 4 | 16px | p-4 / gap-4 | 카드 내부 |
| 6 | 24px | p-6 / gap-6 | 섹션 간격 |
| 8 | 32px | gap-8 | 쿼터 카드 간격 |

---

## 5. 레이아웃

### 반응형 브레이크포인트
```css
sm: 640px   /* 모바일 가로 */
md: 768px   /* 태블릿 - 축구장 가로 전환 */
lg: 1024px  /* 데스크탑 */
```

### 축구장 비율
```
가로:세로 = 3:2 (실제 축구장 비율 105m x 68m 근사)
aspect-ratio: 3/2
max-width: 600px (모바일), 800px (데스크탑)
```

---

## 6. 컴포넌트 라이브러리

### 사용 컴포넌트 (shadcn/ui)
```bash
npx shadcn-ui@latest add button
npx shadcn-ui@latest add input
npx shadcn-ui@latest add card
npx shadcn-ui@latest add dialog
npx shadcn-ui@latest add tabs
npx shadcn-ui@latest add slider
npx shadcn-ui@latest add badge
```

### 커스텀 컴포넌트

#### 1. SoccerField (축구장)
```tsx
<SoccerField>
  {/* 축구장 배경 + 라인 */}
  <div className="relative w-full aspect-[3/2] bg-[#2D5A27] rounded-lg border-2 border-white">
    {/* 센터 서클 */}
    <div className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2
                    w-24 h-24 rounded-full border-2 border-white/50" />
    {/* 센터 라인 */}
    <div className="absolute left-1/2 top-0 bottom-0 w-0.5 bg-white/50" />
    {/* 페널티 박스 (상단/하단) */}
    {/* 선수 마커들 */}
    {players.map(player => (
      <PlayerMarker key={player.id} player={player} />
    ))}
  </div>
</SoccerField>
```

#### 2. PlayerMarker (드래그 가능 선수 마커)
```tsx
<div
  className="absolute w-10 h-10 -ml-5 -mt-5 cursor-grab active:cursor-grabbing
             rounded-full flex items-center justify-center text-white font-bold
             shadow-lg transition-transform hover:scale-110"
  style={{
    left: `${positionX}%`,
    top: `${positionY}%`,
    backgroundColor: positionColor // GK: amber, DF: blue, MF: emerald, FW: red
  }}
>
  {playerNumber}
</div>
```

#### 3. QuarterTabs (쿼터 탭)
```tsx
<Tabs defaultValue="q1">
  <TabsList className="grid grid-cols-4 w-full">
    <TabsTrigger value="q1">1쿼터</TabsTrigger>
    <TabsTrigger value="q2">2쿼터</TabsTrigger>
    <TabsTrigger value="q3">3쿼터</TabsTrigger>
    <TabsTrigger value="q4">4쿼터</TabsTrigger>
  </TabsList>
  <TabsContent value="q1">
    <SoccerField quarter={1} />
    <PlayerRatingList quarter={1} />
  </TabsContent>
  {/* ... */}
</Tabs>
```

#### 4. RatingInput (점수 입력)
```tsx
<div className="flex items-center gap-3">
  <Slider
    min={0}
    max={10}
    step={0.1}
    value={[rating]}
    onValueChange={(v) => setRating(v[0])}
    className="flex-1"
  />
  <span className="w-12 text-2xl font-mono font-bold text-right">
    {rating.toFixed(1)}
  </span>
</div>
```

#### 5. StatInput (통계 입력)
```tsx
<div className="grid grid-cols-4 gap-2 text-center">
  <div>
    <Label className="text-xs">골</Label>
    <Input type="number" min={0} value={goals} className="text-center" />
  </div>
  <div>
    <Label className="text-xs">어시</Label>
    <Input type="number" min={0} value={assists} className="text-center" />
  </div>
  <div>
    <Label className="text-xs">클린시트</Label>
    <Checkbox checked={cleanSheet} />
  </div>
  <div>
    <Label className="text-xs">기여도</Label>
    <Input type="number" min={0} max={10} step={0.1} value={contribution} />
  </div>
</div>
```

#### 6. MVPBadge (MVP 배지)
```tsx
<Badge className="bg-amber-400 text-amber-900 font-bold">
  MVP
</Badge>
```

---

## 7. 아이콘 시스템

### 사용 아이콘 (lucide-react)
```tsx
import {
  Home,           // 홈
  Users,          // 선수 관리
  Trophy,         // 경기 목록
  Plus,           // 추가
  Save,           // 저장
  ArrowLeft,      // 뒤로가기
  Star,           // MVP
  Settings,       // 설정
  LogOut,         // 로그아웃
  GripVertical,   // 드래그 핸들
} from 'lucide-react'
```

---

## 8. 주요 화면 와이어프레임

### 8.1 랜딩 페이지 `/`
```
┌─────────────────────────────────────┐
│  [Logo]                    [로그인] │
├─────────────────────────────────────┤
│                                     │
│        SoccerNote                   │
│        축구팀 경기 기록의 모든 것     │
│                                     │
│        [시작하기 버튼]               │
│                                     │
├─────────────────────────────────────┤
│  [Feature 1]  [Feature 2]  [Feature 3]
│   포메이션      쿼터별       자동 MVP
│   배치         점수 기록      선정
└─────────────────────────────────────┘
```

### 8.2 대시보드 `/dashboard`
```
┌─────────────────────────────────────┐
│  SoccerNote    [팀: 우리팀 ▼] [설정]│
├─────────────────────────────────────┤
│  최근 경기                [+ 새 경기]│
│  ─────────────────────────────────  │
│  ┌───────────────────────────────┐ │
│  │ vs 상대팀 A     2024.01.20    │ │
│  │ 3:2 승리       MVP: 홍길동    │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │ vs 상대팀 B     2024.01.13    │ │
│  │ 1:1 무승부     MVP: 김철수    │ │
│  └───────────────────────────────┘ │
├─────────────────────────────────────┤
│  [홈]    [경기]    [선수]    [설정]  │
└─────────────────────────────────────┘
```

### 8.3 선수 관리 `/team/players`
```
┌─────────────────────────────────────┐
│  ← 선수 관리              [+ 선수]  │
├─────────────────────────────────────┤
│  ┌──────┐ 홍길동 (#10)              │
│  │ FW   │ 평균 평점: 7.5            │
│  └──────┘ 골: 5 / 어시: 3           │
│  ────────────────────────────────── │
│  ┌──────┐ 김철수 (#7)               │
│  │ MF   │ 평균 평점: 6.8            │
│  └──────┘ 골: 2 / 어시: 7           │
│  ────────────────────────────────── │
│  ┌──────┐ 이영희 (#1)               │
│  │ GK   │ 평균 평점: 8.0            │
│  └──────┘ 클린시트: 3               │
└─────────────────────────────────────┘
```

### 8.4 경기 상세 `/match/[id]`
```
┌─────────────────────────────────────┐
│  ←        vs 상대팀 A     [편집]    │
├─────────────────────────────────────┤
│  2024년 1월 20일 | 홈구장           │
│  ────────────────────────────────── │
│       3  :  2                       │
│      우리팀    상대팀                │
│  ────────────────────────────────── │
│  [1쿼터] [2쿼터] [3쿼터] [4쿼터]     │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │      축구장 포메이션 뷰      │   │
│  │   ⚽ (선수들 위치 표시)      │   │
│  │                             │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  MVP: ⭐ 홍길동 (평균 8.5점)        │
├─────────────────────────────────────┤
│  선수별 기록                        │
│  ┌────────────────────────────┐    │
│  │ #10 홍길동  평점: 8.5       │    │
│  │ FW  골: 2, 어시: 1          │    │
│  └────────────────────────────┘    │
└─────────────────────────────────────┘
```

### 8.5 쿼터 편집 `/match/[id]/quarter/[q]`
```
┌─────────────────────────────────────┐
│  ←        1쿼터 편집       [저장]   │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │      축구장 (드래그 가능)    │   │
│  │                             │   │
│  │   🟡        ← GK            │   │
│  │  🔵 🔵 🔵 🔵  ← DF          │   │
│  │   🟢 🟢 🟢   ← MF           │   │
│  │    🔴 🔴 🔴  ← FW           │   │
│  │                             │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  선수 배치                          │
│  [선수 선택 ▼] → 필드에 드래그      │
├─────────────────────────────────────┤
│  선수 평가                          │
│  ────────────────────────────────── │
│  홍길동 (#10) FW                    │
│  평점: [────────●──] 8.5           │
│  골: [2] 어시: [1] 기여도: [8.0]   │
│  ────────────────────────────────── │
│  김철수 (#7) MF                     │
│  평점: [───────●───] 7.0           │
│  골: [0] 어시: [1] 기여도: [6.5]   │
└─────────────────────────────────────┘
```

---

## 9. 상태별 디자인

### 선수 마커 상태
- **Default**: 포지션 색상, 등번호 표시
- **Dragging**: scale-110, shadow-xl, opacity-80
- **Selected**: 흰색 테두리 + 펄스 애니메이션
- **Unassigned**: 회색 (gray-400)

### 점수 슬라이더 상태
- **0-3점**: 빨간색 (red-500)
- **4-6점**: 노란색 (amber-500)
- **7-10점**: 초록색 (emerald-500)

### 쿼터 탭 상태
- **Active**: primary 색상, 밑줄
- **Completed**: 체크 아이콘
- **Incomplete**: 점 표시

---

## 10. 애니메이션 & 전환

### 드래그 앤 드롭
```tsx
// @dnd-kit 사용
const animateLayoutChanges = () => ({
  duration: 200,
  easing: 'ease-out',
});
```

### 점수 변경
```tsx
// 숫자 카운트 애니메이션
<motion.span
  key={rating}
  initial={{ scale: 1.2, color: 'green' }}
  animate={{ scale: 1, color: 'currentColor' }}
  transition={{ duration: 0.2 }}
>
  {rating.toFixed(1)}
</motion.span>
```

### MVP 발표
```tsx
// 컨페티 효과 (react-confetti 사용)
<Confetti
  width={width}
  height={height}
  recycle={false}
  numberOfPieces={200}
/>
```

---

## 11. 접근성 체크리스트

- [x] 색상 대비 4.5:1 이상
- [x] 드래그 앤 드롭 키보드 대안 (방향키)
- [x] 포커스 상태 명확히 표시
- [x] 터치 타겟 최소 44x44px
- [x] 점수 입력 숫자 키패드 지원

---

## 12. 디자인 토큰 (Tailwind Config)

```javascript
// tailwind.config.js
const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    extend: {
      colors: {
        primary: colors.emerald,
        field: '#2D5A27',
        position: {
          gk: colors.amber[500],
          df: colors.blue[500],
          mf: colors.emerald[500],
          fw: colors.red[500],
        }
      },
      fontFamily: {
        sans: ['Pretendard', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
      aspectRatio: {
        'field': '3 / 2',
      },
    }
  }
}
```

---

## 13. 다음 단계

- [ ] 이 디자인 스펙 검토 및 승인
- [ ] Supabase 프로젝트 생성
- [ ] Next.js 프로젝트 초기화 및 개발 시작
